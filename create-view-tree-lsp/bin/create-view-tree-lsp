#!/usr/bin/env node
const { execFileSync } = require('child_process')

try {
	execFileSync('npx', ['view-tree-lsp', 'create', ...process.argv.slice(2)], { stdio: 'inherit' })
} catch {
	process.exit(1)
}
