#!/usr/bin/env node
const { execFileSync } = require('child_process')

try {
	execFileSync('npx', ['--yes', 'view-tree-lsp@latest', 'create', ...process.argv.slice(2)], { stdio: 'inherit' })
} catch {
	process.exit(1)
}
