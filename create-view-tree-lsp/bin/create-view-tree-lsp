#!/usr/bin/env node
const { execSync } = require('child_process')

const args = process.argv.slice(2).join(' ')

try {
	execSync(`npx view-tree-lsp create ${args}`, { stdio: 'inherit' })
} catch {
	process.exit(1)
}
