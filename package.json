{
	"name": "view.tree",
	"version": "0.1.0",
	"scripts": {
		"prestart": "tree-sitter build --wasm",
		"tree": "tree-sitter",
		"start": "tree-sitter playground"
	},
	"dependencies": {
		"tree-sitter": "^0.25.0"
	}
}
