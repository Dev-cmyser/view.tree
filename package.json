{
	"name": "view.tree",
	"version": "0.1.0",
	"scripts": {
		"prestart": "cd tree-sitter && tree-sitter build --wasm",
		"start": " cd tree-sitter && tree-sitter playground",
		"tree": " cd tree-sitter && tree-sitter",
		"gen": " cd tree-sitter && tree-sitter generate && npm i "
	},
	"dependencies": {
		"tree-sitter": "^0.25.0",
		"vscode-languageserver": "^9.0.1",
		"vscode-languageserver-textdocument": "^1.0.12"
	},
	"devDependencies": {
		"@types/node": "^24.3.1",
		"typescript": "^5.9.2"
	}
}
